@model AsignarCuentaViewModel
@{
    ViewData["Title"] = "Asignar cuenta de ahorro";
}

<h2 class="abp-page-title">Asignar cuenta de ahorro secundaria</h2>
<div class="abp-divider-accent"></div>

<form asp-action="Asignar" method="post">
    <div asp-validation-summary="ModelOnly" class="text-validation mb-2"></div>
    <span asp-validation-for="ClientId" class="text-validation d-block mb-2"></span>

    <div class="card mb-3">
        <div class="card-header">Seleccione el cliente</div>
        <div class="table-responsive">
            <table class="table abp-table mb-0">
                <thead>
                <tr>
                    <th></th>
                    <th>Cédula</th>
                    <th>Nombre</th>
                    <th>Correo</th>
                </tr>
                </thead>
                <tbody>
                @if (Model.Clientes.Count == 0)
                {
                    <tr><td colspan="4" class="text-center text-muted py-4">No hay clientes activos.</td></tr>
                }
                @foreach (var cliente in Model.Clientes)
                {
                    <tr>
                        <td>
                            <input class="form-check-input" type="radio" name="ClientId" value="@cliente.Id"
                                   checked="@(Model.ClientId == cliente.Id)" />
                        </td>
                        <td>@cliente.Identification</td>
                        <td>@cliente.FullName</td>
                        <td>@cliente.Email</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>

    <div class="row justify-content-start">
        <div class="col-lg-4 mb-3">
            <label asp-for="InitialBalance" class="form-label"></label>
            <input asp-for="InitialBalance" class="form-control" type="number" step="0.01" min="0" />
            <span asp-validation-for="InitialBalance" class="text-validation"></span>
        </div>
    </div>

    <div class="d-flex justify-content-end gap-2">
        <a class="btn btn-outline-secondary" asp-action="Index">Cancelar</a>
        <button type="submit" class="btn btn-primary" @(Model.Clientes.Count == 0 ? "disabled" : null)>Asignar cuenta</button>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
